---
phase: 02-mcp-server-model-io
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mdf-server/pyproject.toml
  - mdf-server/mdf_server/server.py
  - mdf-server/mdf_server/tools/__init__.py
  - mdf-server/mdf_server/tools/model_io.py
  - mdf-server/mdf_server/tools/drawio.py
  - mdf-server/mdf_server/tools/validation.py
  - mdf-server/mdf_server/tools/simulation.py
  - mdf-server/mdf_server/pycca/__init__.py
  - mdf-server/tests/test_model_io.py
autonomous: true
requirements:
  - MCP-00

must_haves:
  truths:
    - "Running `cd mdf-server && uv run python -c 'import mdf_server.server'` exits 0"
    - "`cd mdf-server && uv run pytest tests/test_model_io.py::test_imports -x` passes"
    - "All 9 test cases in test_model_io.py are collected by pytest (7 skipped, 2 pass)"
    - "Running `cd mdf-server && uv run pytest tests/ -x` exits 0 (full suite still green)"
  artifacts:
    - path: "mdf-server/pyproject.toml"
      provides: "fastmcp>=3.1.0,<4.0.0 in [project] dependencies and [project.scripts] mdf-server entry point"
    - path: "mdf-server/mdf_server/server.py"
      provides: "FastMCP('mdf') instance, mcp.tool() registrations, main() calling mcp.run()"
    - path: "mdf-server/mdf_server/tools/model_io.py"
      provides: "Stub module with list_domains, read_model, write_model signatures and docstrings — no implementation yet"
    - path: "mdf-server/mdf_server/tools/drawio.py"
      provides: "Stub module referencing Phase 4"
    - path: "mdf-server/mdf_server/tools/validation.py"
      provides: "Stub module referencing Phase 3"
    - path: "mdf-server/mdf_server/tools/simulation.py"
      provides: "Stub module referencing Phase 5"
    - path: "mdf-server/mdf_server/pycca/__init__.py"
      provides: "Stub package referencing Phase 5"
    - path: "mdf-server/tests/test_model_io.py"
      provides: "9 test cases: test_imports passes; 7 behavioral tests skip with plan reference"
  key_links:
    - from: "mdf-server/mdf_server/server.py"
      to: "mdf-server/mdf_server/tools/model_io.py"
      via: "mcp.tool(list_domains), mcp.tool(read_model), mcp.tool(write_model)"
      pattern: "mcp\\.tool\\(list_domains\\)"
    - from: "mdf-server/pyproject.toml"
      to: "mdf_server.server:main"
      via: "[project.scripts] mdf-server entry"
      pattern: "mdf-server.*mdf_server\\.server:main"
---

<objective>
Scaffold the FastMCP server package: add fastmcp dependency and entry point to pyproject.toml, create server.py, stub all tool modules, stub the pycca package, and write the complete test_model_io.py test file with 9 test cases (7 skipped pending plan 02-02 implementation).

Purpose: Establishes the Nyquist Wave 0 contract — all tests exist and are collected before any implementation is written. Prevents test/implementation drift.
Output: Importable server package, stub tool modules following Phase 1 stub pattern, complete test file with skip markers.
</objective>

<execution_context>
@C:/Users/bchaloux/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bchaloux/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-mcp-server-model-io/02-CONTEXT.md
@.planning/phases/02-mcp-server-model-io/02-RESEARCH.md

<interfaces>
<!-- Key types and patterns the executor needs. Extracted from Phase 1 codebase. -->

From mdf-server/mdf_server/schema/yaml_schema.py (used in plan 02-02 by write_model):
```python
# ClassDiagramFile is the top-level Pydantic model for class-diagram.yaml
# Usage: ClassDiagramFile.model_validate(data)  # data = yaml.safe_load(yaml_str)
```

Phase 1 stub pattern (from 01-01-PLAN.md):
```python
"""
<ModuleName> — <purpose description>

Implemented in plan NN-NN.
"""
# (no imports, no code beyond docstring)
```

Phase 1 test skip pattern:
```python
@pytest.mark.skip(reason="Implemented in plan 02-02")
def test_behavior():
    ...
```

FastMCP registration pattern (from 02-RESEARCH.md):
```python
from fastmcp import FastMCP
from mdf_server.tools.model_io import list_domains, read_model, write_model

mcp = FastMCP("mdf")

mcp.tool(list_domains)
mcp.tool(read_model)
mcp.tool(write_model)

def main() -> None:
    mcp.run()

if __name__ == "__main__":
    main()
```

model_io.py stub signatures (for server.py to import):
```python
def list_domains() -> list[str]: ...
def read_model(domain: str) -> str | dict: ...
def write_model(domain: str, yaml_str: str) -> list[dict]: ...
```

Test fixture pattern (from 02-RESEARCH.md):
```python
VALID_CLASS_DIAGRAM_YAML = """\
schema_version: "1.0.0"
domain: Hydraulics
classes:
  - name: Valve
    stereotype: entity
    attributes:
      - name: valve_id
        type: UniqueID
        identifier: true
associations: []
bridges: []
"""

def test_list_domains_empty(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)  # no .design/model/ created
    from mdf_server.tools.model_io import list_domains
    assert list_domains() == []
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold server package — pyproject.toml, server.py, stub modules</name>
  <files>
    mdf-server/pyproject.toml,
    mdf-server/mdf_server/server.py,
    mdf-server/mdf_server/tools/__init__.py,
    mdf-server/mdf_server/tools/model_io.py,
    mdf-server/mdf_server/tools/drawio.py,
    mdf-server/mdf_server/tools/validation.py,
    mdf-server/mdf_server/tools/simulation.py,
    mdf-server/mdf_server/pycca/__init__.py
  </files>
  <action>
    1. Edit `mdf-server/pyproject.toml`:
       - Add `"fastmcp>=3.1.0,<4.0.0"` to `[project] dependencies` list (after ruamel-yaml line)
       - Add new section after `[dependency-groups]`:
         ```toml
         [project.scripts]
         mdf-server = "mdf_server.server:main"
         ```

    2. Create `mdf-server/mdf_server/server.py`:
       ```python
       """MDF MCP server — FastMCP entry point."""
       from fastmcp import FastMCP
       from mdf_server.tools.model_io import list_domains, read_model, write_model

       mcp = FastMCP("mdf")

       mcp.tool(list_domains)
       mcp.tool(read_model)
       mcp.tool(write_model)


       def main() -> None:
           """Start the MDF MCP server with stdio transport."""
           mcp.run()


       if __name__ == "__main__":
           main()
       ```

    3. Create `mdf-server/mdf_server/tools/__init__.py` — empty file (package marker).

    4. Create `mdf-server/mdf_server/tools/model_io.py` — stub only, following Phase 1 stub pattern. Must define all three function signatures with type annotations and contract docstrings (FastMCP uses them as tool descriptions). Bodies return minimal valid values (empty list / empty string / empty list) so server.py imports cleanly. Do NOT implement logic — that is plan 02-02.

       ```python
       """
       model_io — MDF model I/O tools: list_domains, read_model, write_model.

       Full implementation in plan 02-02.
       """
       from pathlib import Path

       MODEL_ROOT = Path(".design/model")


       def list_domains() -> list[str]:
           """
           Returns all domain names found in .design/model/.
           A domain is any subdirectory containing class-diagram.yaml.
           Returns [] if .design/model/ does not exist or is empty.
           Call this before read_model() to discover available domains.
           """
           return []  # implemented in plan 02-02


       def read_model(domain: str) -> str | dict:
           """
           Returns the raw YAML string from .design/model/<domain>/class-diagram.yaml.
           Domain lookup is case-insensitive.
           On success: returns the YAML file content as a string.
           On domain not found: returns {"error": "Domain 'X' not found", "available": [...]}
           Call list_domains() first to discover available domain names.
           """
           return ""  # implemented in plan 02-02


       def write_model(domain: str, yaml_str: str) -> list[dict]:
           """
           Saves yaml_str to .design/model/<domain>/class-diagram.yaml after validation.
           Validates YAML syntax and Pydantic schema before writing. Never writes on error.
           Auto-creates .design/model/<domain>/ if it does not exist.
           Returns [] on success.
           Returns [{"issue": str, "location": str, "value": any}] on any error.
           Never raises exceptions — all errors are returned as structured data.
           """
           return []  # implemented in plan 02-02
       ```

    5. Create `mdf-server/mdf_server/tools/drawio.py` — stub:
       ```python
       """
       drawio — Draw.io rendering, validation, and sync tools.

       Implemented in plan 04-xx (Phase 4).
       """
       ```

    6. Create `mdf-server/mdf_server/tools/validation.py` — stub:
       ```python
       """
       validation — Model validation tool (validate_model).

       Implemented in plan 03-xx (Phase 3).
       """
       ```

    7. Create `mdf-server/mdf_server/tools/simulation.py` — stub:
       ```python
       """
       simulation — State machine simulation tool (simulate_state_machine).

       Implemented in plan 05-xx (Phase 5).
       """
       ```

    8. Create `mdf-server/mdf_server/pycca/__init__.py` — stub:
       ```python
       """
       pycca — pycca action language parser and interpreter.

       Implemented in plan 05-xx (Phase 5).
       """
       ```

    Run `cd mdf-server && uv add "fastmcp>=3.1.0,<4.0.0"` to sync the lock file. Then verify: `uv run python -c "import mdf_server.server"` must exit 0.
  </action>
  <verify>
    <automated>cd /c/Users/bchaloux/Local_Documents/Local_Repos/model-based-project-framework/mdf-server && uv run python -c "import mdf_server.server; print('OK')"</automated>
  </verify>
  <done>
    - `mdf-server/mdf_server/server.py` exists and imports without error
    - `pyproject.toml` contains `fastmcp>=3.1.0,<4.0.0` in dependencies and `[project.scripts]` section
    - All 5 stub files exist (tools/drawio.py, validation.py, simulation.py, pycca/__init__.py, tools/__init__.py)
    - `uv run python -c "import mdf_server.server"` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Write test_model_io.py with all 9 test cases (7 skipped)</name>
  <files>mdf-server/tests/test_model_io.py</files>
  <action>
    Create `mdf-server/tests/test_model_io.py`. The file defines all 9 test cases from the VALIDATION.md requirement map.

    - `test_imports` — verifies server module and all tool modules are importable. Must pass immediately (no skip).
    - 7 behavioral tests — each decorated with `@pytest.mark.skip(reason="Implemented in plan 02-02")`. They define the full assertion bodies (not empty) so they become executable after skip removal in plan 02-02.

    Use `monkeypatch.chdir(tmp_path)` for CWD isolation in all behavioral tests. Import tool functions inside each test function body (not at module level) so CWD changes via monkeypatch take effect before the MODULE_ROOT constant is evaluated.

    ```python
    """Tests for MCP model I/O tools: list_domains, read_model, write_model."""
    import pytest
    from pathlib import Path

    # ---------------------------------------------------------------------------
    # Fixtures
    # ---------------------------------------------------------------------------

    VALID_CLASS_DIAGRAM_YAML = """\
    schema_version: "1.0.0"
    domain: Hydraulics
    classes:
      - name: Valve
        stereotype: entity
        attributes:
          - name: valve_id
            type: UniqueID
            identifier: true
    associations: []
    bridges: []
    """

    INVALID_YAML_SYNTAX = "key: [unclosed"

    SCHEMA_INVALID_YAML = """\
    schema_version: "1.0.0"
    domain: Hydraulics
    classes:
      - name: Valve
        stereotype: not_a_valid_stereotype
    associations: []
    bridges: []
    """


    # ---------------------------------------------------------------------------
    # MCP-00: Import smoke tests
    # ---------------------------------------------------------------------------

    def test_imports():
        """Server and all tool modules are importable without error."""
        import mdf_server.server  # noqa: F401
        import mdf_server.tools.model_io  # noqa: F401
        import mdf_server.tools.drawio  # noqa: F401
        import mdf_server.tools.validation  # noqa: F401
        import mdf_server.tools.simulation  # noqa: F401
        import mdf_server.pycca  # noqa: F401


    # ---------------------------------------------------------------------------
    # MCP-01: list_domains
    # ---------------------------------------------------------------------------

    @pytest.mark.skip(reason="Implemented in plan 02-02")
    def test_list_domains_empty(tmp_path, monkeypatch):
        """list_domains() returns [] when .design/model/ does not exist."""
        monkeypatch.chdir(tmp_path)
        from mdf_server.tools import model_io
        import importlib
        importlib.reload(model_io)
        assert model_io.list_domains() == []


    @pytest.mark.skip(reason="Implemented in plan 02-02")
    def test_list_domains_with_domains(tmp_path, monkeypatch):
        """list_domains() returns domain names for directories containing class-diagram.yaml."""
        monkeypatch.chdir(tmp_path)
        domain_dir = tmp_path / ".design" / "model" / "Hydraulics"
        domain_dir.mkdir(parents=True)
        (domain_dir / "class-diagram.yaml").write_text(VALID_CLASS_DIAGRAM_YAML)
        from mdf_server.tools import model_io
        import importlib
        importlib.reload(model_io)
        result = model_io.list_domains()
        assert result == ["Hydraulics"]


    # ---------------------------------------------------------------------------
    # MCP-02: read_model
    # ---------------------------------------------------------------------------

    @pytest.mark.skip(reason="Implemented in plan 02-02")
    def test_read_model_known(tmp_path, monkeypatch):
        """read_model() returns YAML string for a known domain."""
        monkeypatch.chdir(tmp_path)
        domain_dir = tmp_path / ".design" / "model" / "Hydraulics"
        domain_dir.mkdir(parents=True)
        (domain_dir / "class-diagram.yaml").write_text(VALID_CLASS_DIAGRAM_YAML)
        from mdf_server.tools import model_io
        import importlib
        importlib.reload(model_io)
        result = model_io.read_model("Hydraulics")
        assert isinstance(result, str)
        assert "schema_version" in result


    @pytest.mark.skip(reason="Implemented in plan 02-02")
    def test_read_model_unknown(tmp_path, monkeypatch):
        """read_model() returns error dict with available list for unknown domain."""
        monkeypatch.chdir(tmp_path)
        domain_dir = tmp_path / ".design" / "model" / "Hydraulics"
        domain_dir.mkdir(parents=True)
        (domain_dir / "class-diagram.yaml").write_text(VALID_CLASS_DIAGRAM_YAML)
        from mdf_server.tools import model_io
        import importlib
        importlib.reload(model_io)
        result = model_io.read_model("NonExistent")
        assert isinstance(result, dict)
        assert "error" in result
        assert "available" in result
        assert "Hydraulics" in result["available"]


    # ---------------------------------------------------------------------------
    # MCP-03: write_model
    # ---------------------------------------------------------------------------

    @pytest.mark.skip(reason="Implemented in plan 02-02")
    def test_write_model_valid(tmp_path, monkeypatch):
        """write_model() writes file and returns [] for valid YAML."""
        monkeypatch.chdir(tmp_path)
        from mdf_server.tools import model_io
        import importlib
        importlib.reload(model_io)
        result = model_io.write_model("Hydraulics", VALID_CLASS_DIAGRAM_YAML)
        assert result == []
        written = (tmp_path / ".design" / "model" / "Hydraulics" / "class-diagram.yaml")
        assert written.exists()
        assert "schema_version" in written.read_text()


    @pytest.mark.skip(reason="Implemented in plan 02-02")
    def test_write_model_bad_yaml(tmp_path, monkeypatch):
        """write_model() returns parse error issue for invalid YAML syntax, no file written."""
        monkeypatch.chdir(tmp_path)
        from mdf_server.tools import model_io
        import importlib
        importlib.reload(model_io)
        result = model_io.write_model("Hydraulics", INVALID_YAML_SYNTAX)
        assert isinstance(result, list)
        assert len(result) >= 1
        assert "issue" in result[0]
        assert "YAML parse error" in result[0]["issue"]
        assert not (tmp_path / ".design" / "model" / "Hydraulics" / "class-diagram.yaml").exists()


    @pytest.mark.skip(reason="Implemented in plan 02-02")
    def test_write_model_schema_invalid(tmp_path, monkeypatch):
        """write_model() returns Pydantic issue list for schema-invalid YAML, no file written."""
        monkeypatch.chdir(tmp_path)
        from mdf_server.tools import model_io
        import importlib
        importlib.reload(model_io)
        result = model_io.write_model("Hydraulics", SCHEMA_INVALID_YAML)
        assert isinstance(result, list)
        assert len(result) >= 1
        assert "issue" in result[0]
        assert "location" in result[0]
        assert not (tmp_path / ".design" / "model" / "Hydraulics" / "class-diagram.yaml").exists()
    ```

    Note on import isolation: Because `MODEL_ROOT = Path(".design/model")` is evaluated at module import time, tests use `importlib.reload(model_io)` after `monkeypatch.chdir(tmp_path)` to ensure MODEL_ROOT resolves relative to the test's CWD. This is the safe pattern for CWD-relative module-level constants.
  </action>
  <verify>
    <automated>cd /c/Users/bchaloux/Local_Documents/Local_Repos/model-based-project-framework/mdf-server && uv run pytest tests/test_model_io.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `tests/test_model_io.py` exists with 9 test functions
    - `test_imports` passes (collected and green)
    - 7 behavioral tests are skipped (not failed, not erroring)
    - `pytest tests/ -x` exits 0 (full suite still green including Phase 1 tests)
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
cd /c/Users/bchaloux/Local_Documents/Local_Repos/model-based-project-framework/mdf-server && uv run pytest tests/ -v
```

Expected output: All Phase 1 tests pass, test_imports passes, 7 behavioral tests reported as SKIPPED. Exit code 0.

```bash
cd /c/Users/bchaloux/Local_Documents/Local_Repos/model-based-project-framework/mdf-server && uv run python -c "import mdf_server.server; print('server imports OK')"
```

Expected: prints "server imports OK", exit 0.
</verification>

<success_criteria>
- `pyproject.toml` has `fastmcp>=3.1.0,<4.0.0` in dependencies and `[project.scripts]` section
- `mdf_server/server.py` imports and contains FastMCP("mdf") instance + 3 tool registrations
- 7 stub files created (server.py, tools/__init__.py, tools/model_io.py, tools/drawio.py, tools/validation.py, tools/simulation.py, pycca/__init__.py)
- `tests/test_model_io.py` has 9 tests: 1 passing, 7 skipped, 0 failing
- `pytest tests/ -x` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-mcp-server-model-io/02-01-SUMMARY.md`
</output>
