---
phase: 01-schema-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mdf-server/pyproject.toml
  - mdf-server/mdf_server/__init__.py
  - mdf-server/mdf_server/schema/__init__.py
  - mdf-server/mdf_server/schema/yaml_schema.py
  - mdf-server/mdf_server/schema/drawio_schema.py
  - mdf-server/tests/__init__.py
  - mdf-server/tests/test_yaml_schema.py
  - mdf-server/tests/test_drawio_schema.py
  - mdf-server/tests/test_roundtrip.py
  - mdf-server/tests/test_templates.py
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-02
  - SCHEMA-03
  - SCHEMA-04
  - SCHEMA-05
  - TMPL-01
  - TMPL-02
  - TMPL-03
  - TMPL-04

must_haves:
  truths:
    - "Running `cd mdf-server && uv run pytest tests/ -x -q` exits 0 after scaffold"
    - "mdf_server.schema.yaml_schema and mdf_server.schema.drawio_schema are importable"
    - "All four test files exist and collect without import errors (0 passes, 0 failures — stubs)"
  artifacts:
    - path: "mdf-server/pyproject.toml"
      provides: "uv-managed package config with pydantic, pyyaml, ruamel.yaml, lxml, defusedxml deps and pytest dev dep"
    - path: "mdf-server/mdf_server/schema/yaml_schema.py"
      provides: "stub module — importable, no models defined yet"
    - path: "mdf-server/mdf_server/schema/drawio_schema.py"
      provides: "stub module — importable, no constants defined yet"
    - path: "mdf-server/tests/test_yaml_schema.py"
      provides: "pytest stubs for SCHEMA-01 and SCHEMA-02 tests (skipped or xfail)"
    - path: "mdf-server/tests/test_drawio_schema.py"
      provides: "pytest stub for SCHEMA-03 test"
    - path: "mdf-server/tests/test_roundtrip.py"
      provides: "pytest stub for SCHEMA-04 round-trip test"
    - path: "mdf-server/tests/test_templates.py"
      provides: "pytest stub for SCHEMA-05 and TMPL-01..04 template existence tests"
  key_links:
    - from: "mdf-server/pyproject.toml"
      to: "mdf-server/mdf_server/"
      via: "uv project packages configuration"
      pattern: "packages.*mdf_server"
    - from: "mdf-server/tests/test_yaml_schema.py"
      to: "mdf-server/mdf_server/schema/yaml_schema.py"
      via: "import at top of test file"
      pattern: "from mdf_server.schema.yaml_schema import"
---

<objective>
Bootstrap the mdf-server Python package and lay down all test stubs so subsequent plans can run `pytest -x` immediately after any change.

Purpose: Every downstream plan depends on this scaffold existing. Plans 02, 03, and 05 each fill in one piece; Plan 04 completes the round-trip. None of those can run pytest without this foundation.
Output: An installable uv-managed package, importable schema modules (stubs), and four test files that collect with 0 errors.
</objective>

<execution_context>
@C:/Users/bchaloux/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bchaloux/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-foundation/01-CONTEXT.md
@.planning/phases/01-schema-foundation/01-RESEARCH.md
@.planning/phases/01-schema-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Initialize mdf-server uv package and install dependencies</name>
  <files>mdf-server/pyproject.toml, mdf-server/mdf_server/__init__.py, mdf-server/mdf_server/schema/__init__.py</files>
  <action>
Run from the repo root:

```
mkdir -p mdf-server
cd mdf-server
uv init --name mdf-server --no-workspace
uv add pydantic pyyaml "ruamel.yaml" lxml defusedxml
uv add --dev pytest ruff mypy
```

After `uv init`, the generated `pyproject.toml` will have a `hello.py` or similar default. Replace or delete that default file — the package source is `mdf_server/`.

Edit `pyproject.toml` so it declares the package correctly:
- Under `[project]`, verify `name = "mdf-server"` and `requires-python = ">=3.11"`
- Add a `[tool.pytest.ini_options]` section:
  ```toml
  [tool.pytest.ini_options]
  testpaths = ["tests"]
  ```

Create the package directory tree:
```
mdf-server/
  mdf_server/
    __init__.py          (empty)
    schema/
      __init__.py        (empty)
```

If `uv init` created a `src/` layout instead of flat, adjust accordingly — flat layout is preferred (no `src/` wrapper).
  </action>
  <verify>
    <automated>cd /c/Users/bchaloux/Local_Documents/Local_Repos/model-based-project-framework/mdf-server && uv run python -c "import mdf_server; import mdf_server.schema; print('ok')"</automated>
  </verify>
  <done>Package is importable; `uv run python -c "import mdf_server"` exits 0 with no errors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create stub source modules and test scaffolds</name>
  <files>
    mdf-server/mdf_server/schema/yaml_schema.py,
    mdf-server/mdf_server/schema/drawio_schema.py,
    mdf-server/tests/__init__.py,
    mdf-server/tests/test_yaml_schema.py,
    mdf-server/tests/test_drawio_schema.py,
    mdf-server/tests/test_roundtrip.py,
    mdf-server/tests/test_templates.py
  </files>
  <action>
Create `mdf-server/mdf_server/schema/yaml_schema.py` as a stub:
```python
"""
YAML model schema — Pydantic v2 models for all MDF YAML file types.
Implemented in plan 02 (01-02-PLAN.md).
"""
```

Create `mdf-server/mdf_server/schema/drawio_schema.py` as a stub:
```python
"""
Draw.io canonical bijection constants — style strings for all MDF model elements.
Implemented in plan 03 (01-03-PLAN.md).
"""
```

Create `mdf-server/tests/__init__.py` as empty.

Create `mdf-server/tests/test_yaml_schema.py` with stubs for SCHEMA-01 and SCHEMA-02:
```python
"""Tests for yaml_schema.py — SCHEMA-01 (valid YAML accepted) and SCHEMA-02 (schema_version required)."""
import pytest
from mdf_server.schema import yaml_schema  # noqa: F401 — import smoke test


@pytest.mark.skip(reason="Implemented in plan 02")
def test_valid_domains_yaml_accepted():
    """SCHEMA-01: A valid DOMAINS.yaml dict is accepted by DomainsFile without error."""
    pass


@pytest.mark.skip(reason="Implemented in plan 02")
def test_valid_class_diagram_accepted():
    """SCHEMA-01: A valid class-diagram.yaml dict is accepted by ClassDiagramFile without error."""
    pass


@pytest.mark.skip(reason="Implemented in plan 02")
def test_valid_state_diagram_accepted():
    """SCHEMA-01: A valid state-diagrams/<Class>.yaml dict is accepted by StateDiagramFile without error."""
    pass


@pytest.mark.skip(reason="Implemented in plan 02")
def test_valid_types_yaml_accepted():
    """SCHEMA-01: A valid types.yaml dict is accepted by TypesFile without error."""
    pass


@pytest.mark.skip(reason="Implemented in plan 02")
def test_missing_schema_version_rejected():
    """SCHEMA-02: Any model file dict missing schema_version raises ValidationError."""
    pass


@pytest.mark.skip(reason="Implemented in plan 02")
def test_invalid_semver_rejected():
    """SCHEMA-02: schema_version not matching semver pattern raises ValidationError."""
    pass


@pytest.mark.skip(reason="Implemented in plan 02")
def test_associative_missing_formalizes_rejected():
    """SCHEMA-01: ClassDef with stereotype=associative and no formalizes raises ValidationError."""
    pass


@pytest.mark.skip(reason="Implemented in plan 02")
def test_guard_all_or_none_violation_rejected():
    """SCHEMA-01: StateDiagramFile with mixed guarded/unguarded transitions on same (from, event) raises ValidationError."""
    pass
```

Create `mdf-server/tests/test_drawio_schema.py` with a stub for SCHEMA-03:
```python
"""Tests for drawio_schema.py — SCHEMA-03 (bijection table complete)."""
import pytest
from mdf_server.schema import drawio_schema  # noqa: F401 — import smoke test


@pytest.mark.skip(reason="Implemented in plan 03")
def test_all_yaml_elements_have_style_constant():
    """SCHEMA-03: drawio_schema defines a style constant for every YAML element type."""
    pass


@pytest.mark.skip(reason="Implemented in plan 03")
def test_style_constants_are_nonempty_strings():
    """SCHEMA-03: All style constants are non-empty strings."""
    pass
```

Create `mdf-server/tests/test_roundtrip.py` with a stub for SCHEMA-04:
```python
"""Tests for Draw.io round-trip — SCHEMA-04."""
import pytest


@pytest.mark.skip(reason="Implemented in plan 04")
def test_generated_xml_is_valid_xml():
    """SCHEMA-04: XML generated from a sample model is well-formed XML parseable by defusedxml."""
    pass


@pytest.mark.skip(reason="Implemented in plan 04")
def test_roundtrip_structural_equality():
    """SCHEMA-04: generate XML -> parse back -> compare class/state/transition names are identical."""
    pass
```

Create `mdf-server/tests/test_templates.py` with stubs for SCHEMA-05 and TMPL-01..04:
```python
"""Tests for template files — SCHEMA-05 (behavior docs), TMPL-01..04 (YAML templates)."""
import pytest
from pathlib import Path

REPO_ROOT = Path(__file__).parent.parent.parent


@pytest.mark.skip(reason="Implemented in plan 05")
def test_domains_yaml_template_exists():
    """TMPL-01: templates/DOMAINS.yaml.tmpl exists."""
    pass


@pytest.mark.skip(reason="Implemented in plan 05")
def test_class_diagram_yaml_template_exists():
    """TMPL-02: templates/CLASS_DIAGRAM.yaml.tmpl exists."""
    pass


@pytest.mark.skip(reason="Implemented in plan 05")
def test_state_diagram_yaml_template_exists():
    """TMPL-03: templates/STATE_DIAGRAM.yaml.tmpl exists."""
    pass


@pytest.mark.skip(reason="Implemented in plan 05")
def test_behavior_domain_template_exists():
    """TMPL-04 / SCHEMA-05: templates/behavior-domain.md.tmpl exists."""
    pass


@pytest.mark.skip(reason="Implemented in plan 05")
def test_behavior_class_template_exists():
    """TMPL-04 / SCHEMA-05: templates/behavior-class.md.tmpl exists."""
    pass


@pytest.mark.skip(reason="Implemented in plan 05")
def test_behavior_state_template_exists():
    """TMPL-04 / SCHEMA-05: templates/behavior-state.md.tmpl exists."""
    pass


@pytest.mark.skip(reason="Implemented in plan 05")
def test_behavior_domain_required_sections():
    """SCHEMA-05: behavior-domain.md.tmpl contains Purpose, Scope, Bridge Contracts sections."""
    pass


@pytest.mark.skip(reason="Implemented in plan 05")
def test_behavior_class_required_sections():
    """SCHEMA-05: behavior-class.md.tmpl contains Purpose, Attributes, Lifecycle, Methods sections."""
    pass


@pytest.mark.skip(reason="Implemented in plan 05")
def test_behavior_state_required_sections():
    """SCHEMA-05: behavior-state.md.tmpl contains Purpose, States, Event Catalog sections."""
    pass
```
  </action>
  <verify>
    <automated>cd /c/Users/bchaloux/Local_Documents/Local_Repos/model-based-project-framework/mdf-server && uv run pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>
`pytest tests/ -x -q` collects all four test files, reports all tests as skipped (0 failed, 0 errors), and exits 0.
The two stub modules are importable: `from mdf_server.schema import yaml_schema, drawio_schema` succeeds.
  </done>
</task>

</tasks>

<verification>
After both tasks:

1. `cd mdf-server && uv run python -c "from mdf_server.schema import yaml_schema, drawio_schema; print('imports ok')"` exits 0.
2. `cd mdf-server && uv run pytest tests/ -x -q` collects 17 tests, all skipped, 0 errors.
3. `mdf-server/pyproject.toml` exists with pydantic, pyyaml, ruamel.yaml, lxml, defusedxml in `[project.dependencies]` and pytest in `[dependency-groups]` dev.
4. `mdf-server/mdf_server/schema/yaml_schema.py` and `drawio_schema.py` both exist.
5. All four test files exist under `mdf-server/tests/`.
</verification>

<success_criteria>
- uv package is installed and importable
- All dependencies installed (pydantic, pyyaml, ruamel.yaml, lxml, defusedxml, pytest, ruff, mypy)
- pytest collects all stubs with 0 errors, all skipped
- Plans 02, 03, 04, 05 can immediately run `pytest -x` without setup
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-foundation/01-01-SUMMARY.md`
</output>
