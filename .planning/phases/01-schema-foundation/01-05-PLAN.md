---
phase: 01-schema-foundation
plan: 05
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - templates/DOMAINS.yaml.tmpl
  - templates/CLASS_DIAGRAM.yaml.tmpl
  - templates/STATE_DIAGRAM.yaml.tmpl
  - templates/behavior-domain.md.tmpl
  - templates/behavior-class.md.tmpl
  - templates/behavior-state.md.tmpl
  - mdf-server/tests/test_templates.py
autonomous: true
requirements:
  - SCHEMA-05
  - TMPL-01
  - TMPL-02
  - TMPL-03
  - TMPL-04

must_haves:
  truths:
    - "templates/ directory at repo root contains exactly 6 template files"
    - "DOMAINS.yaml.tmpl is valid YAML with schema_version, domains list, and bridges list"
    - "CLASS_DIAGRAM.yaml.tmpl is valid YAML with schema_version, domain, classes list, associations list, bridges list"
    - "STATE_DIAGRAM.yaml.tmpl is valid YAML with schema_version, domain, class field, events list, states list, transitions list"
    - "behavior-domain.md.tmpl contains sections: Purpose, Scope, Bridge Contracts"
    - "behavior-class.md.tmpl contains sections: Purpose, Attributes, Lifecycle, Methods"
    - "behavior-state.md.tmpl contains sections: Purpose, States, Event Catalog"
    - "test_templates.py tests pass (not skipped)"
  artifacts:
    - path: "templates/DOMAINS.yaml.tmpl"
      provides: "Populated DOMAINS.yaml example used by /mdf:new-project skill (Phase 9)"
      contains: "schema_version"
    - path: "templates/CLASS_DIAGRAM.yaml.tmpl"
      provides: "Populated class-diagram.yaml scaffold with entity, active, associative examples"
      contains: "schema_version"
    - path: "templates/STATE_DIAGRAM.yaml.tmpl"
      provides: "Populated state diagram scaffold with events, states, transitions, guards"
      contains: "schema_version"
    - path: "templates/behavior-domain.md.tmpl"
      provides: "Behavior doc scaffold for domain-level description"
      contains: "## Purpose"
    - path: "templates/behavior-class.md.tmpl"
      provides: "Behavior doc scaffold for class-level description"
      contains: "## Attributes"
    - path: "templates/behavior-state.md.tmpl"
      provides: "Behavior doc scaffold for state machine description"
      contains: "## Event Catalog"
  key_links:
    - from: "mdf-server/tests/test_templates.py"
      to: "templates/"
      via: "Path(__file__).parent.parent.parent / 'templates' — relative to test file"
      pattern: "REPO_ROOT.*templates"
---

<objective>
Create all six template files in templates/ at the repo root, then fill in the test_templates.py stubs so SCHEMA-05 and TMPL-01..04 tests pass.

Purpose: Templates are the seed content that /mdf:new-project (Phase 9) copies into a new project's .design/ directory. They must be valid, populated examples — not empty skeletons — so the skill can copy them directly without modification.
Output: 6 template files; 9 passing tests in test_templates.py.
</objective>

<execution_context>
@C:/Users/bchaloux/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bchaloux/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-schema-foundation/01-CONTEXT.md
@.planning/phases/01-schema-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Exact content structure from CONTEXT.md — implement these exactly -->

DOMAINS.yaml structure from CONTEXT.md:
```yaml
schema_version: "1.0.0"
domains:
  - name: <DomainName>
    type: application        # application | realized
    description: <What this domain is responsible for>
bridges:
  - from: <RequiringDomain>
    to: <ProvidingDomain>
    operations:
      - name: <operation_name>
        params:
          - name: <param_name>
            type: <Type>
        return: <ReturnType>   # null if void
```

behavior-domain.md template structure from CONTEXT.md:
```markdown
# Domain: <DomainName>

## Purpose
[One-paragraph statement of this domain's responsibility]

## Scope
### Does
- [Responsibility 1]

### Does Not
- [Out of scope 1]

## Bridge Contracts
### R: <OtherDomain>::<operation_name>
- Input: <param> (<type>)
- Output: <return_type>
- Behavior: [what happens]

### P: <operation_name>
- Input: <param> (<type>)
- Output: <return_type>
- Behavior: [what this domain provides when called]
```

behavior-class.md template structure from CONTEXT.md:
```markdown
# Class: <ClassName>

## Purpose
[What this class represents and its role in the domain]

## Attributes
### <attribute_name>
- Type: <type>
- Changes when: [what causes this value to change]
- Triggers: [events, transitions, or bridge calls that result from a change]

## Lifecycle
[Does this class have a state machine? Key states, what drives transitions.
If none: 'No state machine.']

## Methods
### <method_name>(<params>) -> <return>
[What this method does; pre/post conditions if relevant]
```

behavior-state.md template structure from CONTEXT.md:
```markdown
# State Machine: <ClassName>

## Purpose
[What behavioral role this state machine plays]

## States
### <StateName>
- Invariant: [what is true while in this state]
- Transitions out:
  - <EventName> [<guard>] -> <TargetState>

## Event Catalog
### <EventName>
- Accepted in: [list of states]
- Effect: [what happens]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create all six template files in templates/</name>
  <files>
    templates/DOMAINS.yaml.tmpl,
    templates/CLASS_DIAGRAM.yaml.tmpl,
    templates/STATE_DIAGRAM.yaml.tmpl,
    templates/behavior-domain.md.tmpl,
    templates/behavior-class.md.tmpl,
    templates/behavior-state.md.tmpl
  </files>
  <action>
Create the `templates/` directory at the repo root (same level as `.planning/` and `mdf-server/`).

Write all six template files. Templates are populated examples with realistic placeholder content from the Hydraulics domain — not empty files. This makes them useful as "fill in the blanks" starting points.

**templates/DOMAINS.yaml.tmpl** — Matches CONTEXT.md DOMAINS.yaml structure exactly:
```yaml
schema_version: "1.0.0"
domains:
  - name: <DomainName>
    type: application        # application | realized
    description: <One-sentence description of this domain's responsibility>
  - name: <RealizedDomainName>
    type: realized
    description: <What this realized domain wraps or provides>
bridges:
  - from: <RequiringDomain>
    to: <ProvidingDomain>
    operations:
      - name: <operation_name>
        params:
          - name: <param_name>
            type: <Type>         # Boolean | Integer | Real | String | UniqueID | <CustomType>
        return: <ReturnType>     # null if void
```

**templates/CLASS_DIAGRAM.yaml.tmpl** — Shows entity, active (with state machine), and associative examples:
```yaml
schema_version: "1.0.0"
domain: <DomainName>

classes:
  - name: <EntityClassName>
    stereotype: entity
    attributes:
      - name: <identifier_attr>
        type: UniqueID
        identifier: true
      - name: <regular_attr>
        type: <Type>
        identifier: false

  - name: <ActiveClassName>
    stereotype: active
    attributes:
      - name: <identifier_attr>
        type: UniqueID
        identifier: true
    methods:
      - name: <method_name>
        scope: instance         # instance | class
        params:
          - name: <param_name>
            type: <Type>
        return: null
        action: |
          <pycca action body>

  - name: <AssociativeClassName>
    stereotype: associative
    formalizes: R1              # R-number of the association this class formalizes
    attributes:
      - name: <attr_name>
        type: <Type>
        identifier: false

associations:
  - name: R1
    point_1: <ClassName1>
    point_2: <ClassName2>
    1_mult_2: "1..M"            # "1" | "1..M" | "0..1" | "M"
    2_mult_1: "1"
    1_phrase_2: "<verb phrase>"
    2_phrase_1: "<verb phrase>"

bridges:
  - to_domain: <ProvidingDomainName>
    direction: required
    operations: [<operation_name>]   # names only — signatures in DOMAINS.yaml

  - to_domain: <RequiringDomainName>
    direction: provided
    implementations:
      - name: <operation_name>
        action: |
          <pycca implementation body>
```

**templates/STATE_DIAGRAM.yaml.tmpl** — Shows events, states, transitions with guards:
```yaml
schema_version: "1.0.0"
domain: <DomainName>
class: <ActiveClassName>

events:
  - name: <EventName>
    params:
      - name: <param_name>
        type: <Type>
  - name: <SimpleEventName>
    params: []

states:
  - name: <InitialStateName>
    entry_action: null
  - name: <ActiveStateName>
    entry_action: |
      <pycca entry action — executes when entering this state>
  - name: <FaultStateName>
    entry_action: null

transitions:
  - from: <InitialStateName>
    to: <ActiveStateName>
    event: <EventName>
    guard: "<pycca boolean expression>"    # null if unconditional
    action: null
  - from: <InitialStateName>
    to: <FaultStateName>
    event: <EventName>
    guard: "<complementary guard>"         # if any transition has a guard, ALL must
    action: null
  - from: <ActiveStateName>
    to: <InitialStateName>
    event: <SimpleEventName>
    guard: null
    action: null
```

**templates/behavior-domain.md.tmpl** — Exact section structure from CONTEXT.md:
```markdown
# Domain: <DomainName>

## Purpose
[One-paragraph statement of this domain's responsibility. What problem does it solve? What does
it manage or control? Write in present tense.]

## Scope
### Does
- [Specific responsibility 1]
- [Specific responsibility 2]

### Does Not
- [Explicitly out-of-scope item 1 — helps clarify domain boundaries]
- [Explicitly out-of-scope item 2]

## Bridge Contracts
<!-- List one entry per bridge operation this domain requires or provides. -->
<!-- R: = Required (this domain calls the other domain) -->
<!-- P: = Provided (this domain implements the operation for another domain) -->

### R: <ProvidingDomain>::<operation_name>
- Input: <param_name> (<Type>)
- Output: <ReturnType>
- Behavior: [What this domain expects to happen when it calls this operation]

### P: <operation_name>
- Input: <param_name> (<Type>)
- Output: <ReturnType>
- Behavior: [What this domain does when another domain calls this operation on it]
```

**templates/behavior-class.md.tmpl** — Exact section structure from CONTEXT.md:
```markdown
# Class: <ClassName>

## Purpose
[What real-world thing or concept this class represents. What role does it play in the domain?
One to three sentences.]

## Attributes
### <identifier_attribute_name>
- Type: <Type>
- Changes when: Never — this is the instance identifier.
- Triggers: Nothing directly.

### <regular_attribute_name>
- Type: <Type>
- Changes when: [What event, method, or transition causes this value to change]
- Triggers: [What happens as a result of the change — state transition, bridge call, event, etc.]

## Lifecycle
[Does this class have a state machine? If yes: summarize key states and what drives transitions.
Example: "Active classes cycle through Idle -> Processing -> Complete. The Process event drives
the first transition; a timer expiry or failure drives the others."
If none: "No state machine. Instances are created and deleted but have no behavioral lifecycle."]

## Methods
### <method_name>(<param_name>: <Type>) -> <ReturnType>
[What this method does in one to two sentences. Include pre-conditions (what must be true for
this to be called safely) and post-conditions (what will be true after it returns) if relevant.]
```

**templates/behavior-state.md.tmpl** — Exact section structure from CONTEXT.md:
```markdown
# State Machine: <ClassName>

## Purpose
[What behavioral role this state machine plays. What lifecycle or process does it model?
One to two sentences.]

## States
### <StateName>
- Invariant: [What is always true while an instance is in this state]
- Transitions out:
  - <EventName> [<guard if any>] -> <TargetStateName>
  - <EventName> [<guard if any>] -> <AltTargetStateName>

### <AnotherStateName>
- Invariant: [What is always true while in this state]
- Transitions out:
  - <EventName> -> <TargetStateName>

## Event Catalog
### <EventName>
- Accepted in: [<StateName1>, <StateName2>]
- Effect: [What happens when this event arrives in the listed states — guard evaluation,
  action execution, state transition]

### <AnotherEventName>
- Accepted in: [<StateName>]
- Effect: [What happens]
```

**Important:** Do not add a `# yaml-language-server: $schema=` comment at the top of YAML templates — the Pydantic schema is the validator, not a JSON Schema file. Templates are `.tmpl` files so editors treat them as text by default.
  </action>
  <verify>
    <automated>ls /c/Users/bchaloux/Local_Documents/Local_Repos/model-based-project-framework/templates/ && echo "count: $(ls /c/Users/bchaloux/Local_Documents/Local_Repos/model-based-project-framework/templates/ | wc -l)"</automated>
  </verify>
  <done>templates/ directory exists at repo root with exactly 6 files: DOMAINS.yaml.tmpl, CLASS_DIAGRAM.yaml.tmpl, STATE_DIAGRAM.yaml.tmpl, behavior-domain.md.tmpl, behavior-class.md.tmpl, behavior-state.md.tmpl.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill in test_templates.py with concrete passing tests</name>
  <files>mdf-server/tests/test_templates.py</files>
  <behavior>
    - Each of the 6 existence tests asserts the template file exists at the expected path
    - The 3 section tests assert required section headers are present in the template content
    - No test uses @pytest.mark.skip after this task
  </behavior>
  <action>
Replace all `@pytest.mark.skip` stubs in `mdf-server/tests/test_templates.py` with concrete implementations:

```python
"""Tests for template files — SCHEMA-05 (behavior docs), TMPL-01..04 (YAML templates)."""
import pytest
from pathlib import Path

# Resolve repo root relative to this test file (mdf-server/tests/test_templates.py -> repo root is ../../)
REPO_ROOT = Path(__file__).parent.parent.parent
TEMPLATES_DIR = REPO_ROOT / "templates"


# --- Existence tests (TMPL-01..04) ---

def test_domains_yaml_template_exists():
    """TMPL-01: templates/DOMAINS.yaml.tmpl exists."""
    assert (TEMPLATES_DIR / "DOMAINS.yaml.tmpl").is_file()


def test_class_diagram_yaml_template_exists():
    """TMPL-02: templates/CLASS_DIAGRAM.yaml.tmpl exists."""
    assert (TEMPLATES_DIR / "CLASS_DIAGRAM.yaml.tmpl").is_file()


def test_state_diagram_yaml_template_exists():
    """TMPL-03: templates/STATE_DIAGRAM.yaml.tmpl exists."""
    assert (TEMPLATES_DIR / "STATE_DIAGRAM.yaml.tmpl").is_file()


def test_behavior_domain_template_exists():
    """TMPL-04 / SCHEMA-05: templates/behavior-domain.md.tmpl exists."""
    assert (TEMPLATES_DIR / "behavior-domain.md.tmpl").is_file()


def test_behavior_class_template_exists():
    """TMPL-04 / SCHEMA-05: templates/behavior-class.md.tmpl exists."""
    assert (TEMPLATES_DIR / "behavior-class.md.tmpl").is_file()


def test_behavior_state_template_exists():
    """TMPL-04 / SCHEMA-05: templates/behavior-state.md.tmpl exists."""
    assert (TEMPLATES_DIR / "behavior-state.md.tmpl").is_file()


# --- Section content tests (SCHEMA-05) ---

def test_behavior_domain_required_sections():
    """SCHEMA-05: behavior-domain.md.tmpl contains Purpose, Scope, Bridge Contracts sections."""
    content = (TEMPLATES_DIR / "behavior-domain.md.tmpl").read_text(encoding="utf-8")
    assert "## Purpose" in content
    assert "## Scope" in content
    assert "## Bridge Contracts" in content


def test_behavior_class_required_sections():
    """SCHEMA-05: behavior-class.md.tmpl contains Purpose, Attributes, Lifecycle, Methods sections."""
    content = (TEMPLATES_DIR / "behavior-class.md.tmpl").read_text(encoding="utf-8")
    assert "## Purpose" in content
    assert "## Attributes" in content
    assert "## Lifecycle" in content
    assert "## Methods" in content


def test_behavior_state_required_sections():
    """SCHEMA-05: behavior-state.md.tmpl contains Purpose, States, Event Catalog sections."""
    content = (TEMPLATES_DIR / "behavior-state.md.tmpl").read_text(encoding="utf-8")
    assert "## Purpose" in content
    assert "## States" in content
    assert "## Event Catalog" in content
```
  </action>
  <verify>
    <automated>cd /c/Users/bchaloux/Local_Documents/Local_Repos/model-based-project-framework/mdf-server && uv run pytest tests/test_templates.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>All 9 tests in test_templates.py pass (PASSED, not SKIPPED). 0 failures.</done>
</task>

</tasks>

<verification>
After both tasks:

1. `cd mdf-server && uv run pytest tests/test_templates.py -v` → 9 passed, 0 skipped.
2. `ls templates/` → 6 files present.
3. `cd mdf-server && uv run pytest tests/ -x -q` → all previously passing tests still pass; template tests now pass too.
4. Each YAML template file starts with `schema_version:` as the first non-comment line.
5. Each behavior .md.tmpl file contains the required section headers.
</verification>

<success_criteria>
- 6 template files exist in templates/ at repo root
- YAML templates have schema_version as first field (per CONTEXT.md SCHEMA-02 requirement)
- Behavior doc templates have all required sections per CONTEXT.md behavior doc format
- All 9 template tests pass
- Templates are suitable for direct use by /mdf:new-project skill in Phase 9
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-foundation/01-05-SUMMARY.md`
</output>
